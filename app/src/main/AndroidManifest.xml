<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="VEM.Movil.collapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollApp">
        <activity android:name=".PopupNewStage"></activity>
        <activity android:name=".PopupNewStatus" />
        <activity
            android:name=".New_Project"
            android:label="@string/title_activity_new_project" />
        <activity android:name=".stages" />
        <activity android:name=".activity_newProject" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>